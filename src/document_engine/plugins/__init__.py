# TipTap is a plugin — install separately:
# pip install document-engine-tiptap
#
# Usage:
#   from document_engine.plugins.tiptap import TipTapBuilder
#   builder = TipTapBuilder()
#   doc = builder.build(document)
